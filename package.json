{
  "name": "forenotes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server/index.ts",
    "dev:client": "npm --prefix src/client run dev -- --host 0.0.0.0",
    "dev:full": "node scripts/dev-full.mjs",
    "dev:demo": "tsx watch src/server/devDemo.ts",
    "seed:demo": "tsx src/demo/seedReal.ts",
    "build": "tsc -p tsconfig.json && npm run build:client",
    "build:client": "npm --prefix src/client run build",
    "start": "node dist/server/index.js",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "db:migrate": "tsx src/server/db/migrate.ts"
  },
  "dependencies": {
    "argon2": "^0.44.0",
    "better-sqlite3": "^12.10.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "@types/pg": "^8.15.5",
    "@types/supertest": "^6.0.3",
    "pg-mem": "^3.0.5",
    "supertest": "^7.1.4",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
